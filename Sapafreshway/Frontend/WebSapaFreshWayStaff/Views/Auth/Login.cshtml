@model WebSapaFreshWayStaff.DTOs.Auth.LoginRequest
@{
    Layout = null;
    ViewData["Title"] = "Đăng nhập - SapaFreshWay";
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/design-system.css" />
    <link rel="stylesheet" href="~/css/auth/login.css" />
</head>
<body>
    <div class="login-container">
        <!-- Small logo at the top -->
        <div class="logo-section">
            <img src="~/assets/images/MainLogo.png" alt="logo" style="height:150px;" />
        </div>

        <!-- Centered login form at the bottom -->
        <div class="login-section">
            <div class="login-box">
                <div class="welcome-header">
                    <h2>Chào mừng đến với</h2>
                    <h3><span>SapaFreshWay</span></h3>
                </div>

                <form asp-action="Login" method="post" autocomplete="off">
                    @Html.AntiForgeryToken()

                    <div class="form-group">
                        <label>Vui lòng nhập Email</label>
                        <div class="input-wrapper">
                            <i class="bi bi-envelope-fill"></i>
                            <input asp-for="Email" type="email" class="form-control-custom" placeholder="example@gmail.com" required />
                        </div>
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label>Vui lòng nhập Mật khẩu</label>
                        <div class="input-wrapper">
                            <i class="bi bi-key-fill"></i>
                            <input asp-for="Password" type="password" id="password" class="form-control-custom" placeholder="••••••••" required />
                            <i id="togglePassword" class="bi bi-eye"></i>
                        </div>
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="forgot-password">
                        <a href="@Url.Action("ForgotPassword")">Quên mật khẩu?</a>
                    </div>

                    <button type="submit" class="btn-primary-custom btn-large" style="width: 100%;">Đăng nhập</button>
                </form>
            </div>
        </div>
    </div>

    <!-- Toast Container -->
    <div id="toastContainer" class="toast-container-custom"></div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/toast-notification.js"></script>
    <script src="~/js/auth/login.js"></script>
</body>
</html>
